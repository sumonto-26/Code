/ 38. inclusive_scan() - Inclusive scan (C++17)
    // vector<int> inclusive(vec.size());
    // inclusive_scan(vec.begin(), vec.end(), inclusive.begin());  // Sequential scan
    // cout << "Inclusive scan: ";
    // for (int x : inclusive) cout << x << " ";
    // cout << endl;

    // // 39. exclusive_scan() - Exclusive scan (C++17)
    // vector<int> exclusive(vec.size());
    // exclusive_scan(vec.begin(), vec.end(), exclusive.begin(), 0);  // Sequential scan
    // cout << "Exclusive scan: ";
    // for (int x : exclusive) cout << x << " ";
    // cout << endl;